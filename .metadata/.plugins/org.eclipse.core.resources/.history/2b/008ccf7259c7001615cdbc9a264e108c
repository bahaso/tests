package bahaso.testing.general;

import org.junit.BeforeClass;
import org.junit.Test;

import com.mongodb.BasicDBObject;
import com.mongodb.DB;
import com.mongodb.DBCollection;
import com.mongodb.DBCursor;
import com.mongodb.MongoClient;
import com.mongodb.client.MongoCollection;
import com.mongodb.client.MongoDatabase;

public class cobamongo {
	DB db = null;
	DBCollection collect = null;
	BasicDBObject query = null;
	@BeforeClass
	public void beforeClass(){
		try {
			db = (DB) (new MongoClient("localhost",27017)).getDatabase("bahaso");
			collect = db.getCollection("case");
			System.out.println("Konek database berhasil");
		} catch (Exception e) {
			System.err.println(e.getClass().getName() + ": " + e.getMessage());
		}
	}
	
	@Test
	public void test(){
		query = new BasicDBObject();
		query.put("_id","567925d1938e8e651c8b4579");
		DBCursor cursor = collect.find(query); 
		try {
			DBCollection table = db.getCollection("case");
		} catch (Exception e) {

		}
	}
	
}
